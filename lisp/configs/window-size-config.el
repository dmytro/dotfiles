;; initial window
(setq initial-frame-alist
      '(
        (width . 202) ; character
        (height . 65) ; lines
        ))

;; default/sebsequent window
(setq default-frame-alist
      '(
        (width . 202) ; character
        (height . 65) ; lines
        ))

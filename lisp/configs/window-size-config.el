;; initial window
(setq initial-frame-alist
      '(
        (width . 235) ; character
        (height . 70) ; lines
        ))

;; default/sebsequent window
(setq default-frame-alist
      '(
        (width . 235) ; character
        (height . 70) ; lines
        ))
